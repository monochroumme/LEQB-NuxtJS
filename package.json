{
  "name": "LEQB",
  "version": "1.0.0",
  "description": "Created by Nadir Abdullayev (rarenex) from scratch on Nuxt.js",
  "author": "Nadir",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "cross-env DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "bootstrap-vue": "^2.0.0-rc.11",
    "bootstrap": "^4.1.3"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1",
    "three": "^0.125.0"
  }
}
